# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=1

# OpenAI API Key (required for AI features)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-api-key-here

# GitHub Personal Access Token (optional, for GitHub API integration)
# Get your token from: https://github.com/settings/tokens
# Required scopes: repo (for repository access)
# Never commit this token to source control
GITHUB_TOKEN=your-github-token-here

# Database
DATABASE_PATH=housing_database.db

# Upload folders
UPLOAD_FOLDER=uploads
PROCESSED_FOLDER=processed_images

# Server Configuration
HOST=0.0.0.0
PORT=5000

# Session Configuration
SECRET_KEY=your-secret-key-here-change-in-production

# Email Configuration (optional)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@example.com
SMTP_PASSWORD=your-password

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

# Production Deployment Notes:
# For Railway/Render/Heroku:
# - Set FLASK_ENV=production
# - Set FLASK_DEBUG=0
# - Generate strong SECRET_KEY: python -c "import secrets; print(secrets.token_hex(32))"
# - Add OPENAI_API_KEY for AI features (optional but recommended)
# - Add GITHUB_TOKEN for GitHub API integration (optional)
# - PORT is auto-set by platform, no need to configure
#
# Security Warning:
# - Never commit .env file or expose tokens in public repositories
# - Use environment variables or secrets management in production
# - Rotate tokens regularly and immediately if compromised
