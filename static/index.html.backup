<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تسجيل الدخول - نظام إدارة إسكان أعضاء هيئة التدريس</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Tajawal:700,400&display=swap">
    <style>
        :root {
            --imam-green: #1a5f3f;
            --imam-light-green: #2d7a5a;
            --imam-dark-green: #0f3d28;
            --imam-blue: #0f3d68;
        }

        body {
            font-family: 'Tajawal', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f8f9fa;
            direction: rtl;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #e0e7ef 0%, #f7fcff 100%);
        }

        .login-container {
            width: 100%;
            max-width: 400px;
            padding: 40px;
            background: #fff;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .logo-icon {
            font-size: 4rem;
            color: var(--imam-dark-green);
            margin-bottom: 10px;
        }

        h2 {
            font-weight: 700;
            color: var(--imam-dark-green);
            margin-bottom: 5px;
        }

        .subtitle {
            color: #6c757d;
            margin-bottom: 30px;
        }

        .form-control:focus {
            border-color: var(--imam-light-green);
            box-shadow: 0 0 0 0.25rem rgba(45, 122, 90, 0.25);
        }

        .btn-login {
            background-color: var(--imam-dark-green);
            border-color: var(--imam-dark-green);
            font-weight: 700;
            padding: 10px;
            transition: background-color 0.3s ease;
        }

        .btn-login:hover {
            background-color: var(--imam-light-green);
            border-color: var(--imam-light-green);
        }

        .footer-text {
            margin-top: 20px;
            font-size: 0.8rem;
            color: #adb5bd;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <i class="fas fa-building logo-icon"></i>
        <h2>نظام إدارة إسكان أعضاء هيئة التدريس</h2>
        <p class="subtitle">جامعة الإمام محمد بن سعود الإسلامية</p>
        
        <form id="loginForm">
            <div class="mb-3">
                <input type="text" class="form-control" id="username" placeholder="اسم المستخدم" required value="admin">
            </div>
            <div class="mb-4">
                <input type="password" class="form-control" id="password" placeholder="كلمة المرور" required value="admin123">
            </div>
            <button type="submit" class="btn btn-login w-100">تسجيل الدخول</button>
        </form>

        <div class="footer-text">
            جميع الحقوق محفوظة © 2025 - نظام إدارة الإسكان الجامعي
        </div>
    </div>

    <script>
        document.getElementById('loginForm').addEventListener('submit', function(event) {
            event.preventDefault();
            
            const username = document.getElementById('username').value;
            const password = document.getElementById('password').value;

            // Mock authentication for demonstration
            if (username === 'admin' && password === 'admin123') {
                // Redirect to the main dashboard of the old system
                window.location.href = 'main_dashboard.html';
            } else {
                alert('خطأ في اسم المستخدم أو كلمة المرور.');
            }
        });
    </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
